<h1>Listing book_names</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @book_names.each do |book_name| %>
  <tr>
    <td><%= book_name.name %></td>
    <td><%= link_to 'Show', book_name %></td>
    <td><%= link_to 'Edit', edit_book_name_path(book_name) %></td>
    <td><%= link_to 'Destroy', book_name, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book name', new_book_name_path %>
