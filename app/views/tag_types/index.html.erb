<h1>Listing tag_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Descrition</th>
    <th>Upid</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tag_types.each do |tag_type| %>
  <tr>
    <td><%= tag_type.name %></td>
    <td><%= tag_type.descrition %></td>
    <td><%= tag_type.upid %></td>
    <td><%= link_to 'Show', tag_type %></td>
    <td><%= link_to 'Edit', edit_tag_type_path(tag_type) %></td>
    <td><%= link_to 'Destroy', tag_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tag type', new_tag_type_path %>
