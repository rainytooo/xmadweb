<!DOCTYPE html>
<html>
<head>
  <title>Xmadweb</title>
  
  <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
  <script type="text/javascript" src="<%= Xmadweb::Application.config.staticfile_server_url %>jquery/jquery-ui-1.8.18.custom.min.js"></script>  
  <script type="text/javascript" src="<%= Xmadweb::Application.config.staticfile_server_url %>jquery_ujs.js"></script>
  <link rel="stylesheet" href="<%= Xmadweb::Application.config.staticfile_server_url %>jquery/ui-lightness/jquery-ui-1.8.18.custom.css" type="text/css"/>
  <%= csrf_meta_tags %>
</head>
<body>
	<!-- devise的提示信息 start -->
	<div>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>
	</div>
	<!-- devise的提示信息 end -->
	<!-- 导航 -->

	
	<% if user_signed_in? %>
	<ul>
		<li><p>您好,<%= current_user.name %> &nbsp;<%= admins_only do %>您当前的身份为管理员.<%= link_to "后台管理", admin_root_path %><% end %></p></li>
		<li><%= link_to "学员过程跟踪管理", students_path %></li>
		<li><%= link_to "[退出登录]", destroy_user_session_path, :method => :delete %></li>
	</ul>					
	<% else %>
	<ul>
		<li><%= link_to "[登录]", new_user_session_path, :method => :get %></li>
	</ul>
	<% end %>
	
<%= yield %>

</body>
</html>
