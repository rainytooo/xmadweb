<%= form_for(@student_distribute) do |f| %>
  <% if @student_distribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_distribute.errors.count, "error") %> prohibited this student_distribute from being saved:</h2>

      <ul>
      <% @student_distribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
