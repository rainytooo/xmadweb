<%= form_for(@student_timeline) do |f| %>
  <% if @student_timeline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_timeline.errors.count, "error") %> prohibited this student_timeline from being saved:</h2>

      <ul>
      <% @student_timeline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
