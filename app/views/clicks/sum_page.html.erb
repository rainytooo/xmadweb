<h1>页面统计总表</h1>
<%= link_to '返回', clicks_path %>
<table>
  <tr>
    <th>名称</th>
    <th>点击量</th>
  </tr>

<% @all.each do |p| %>
  <tr>    
  	<td><%= WebPage.find(p.page).domain %></td>
    <td><%= p.sum_page %></td>
  </tr>
<% end %>
</table>